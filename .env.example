# Postgres connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5432/gear_db
# Server bind
HOST=0.0.0.0
PORT=8080
# Logging
RUST_LOG=info
# Optional: set allowed origin (you can handle via reverse proxy too)
CORS_ORIGIN=*
